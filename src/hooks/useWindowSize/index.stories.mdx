import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { useWindowSizeDemo } from './index.stories';

<Meta title="Hooks/useWindowSize" />

# useWindowSize

## Usage

```typescript
const { width, height } = useWindowSize();
```

## Example

<Canvas>
  <Story story={useWindowSizeDemo} />
</Canvas>

Try resizing your window to see the difference.