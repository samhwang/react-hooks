import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { defaultOn, defaultOff } from './index.stories';

<Meta title="Hooks/useToggle" />

# useToggle

## Usage

```typescript
const [isToggled, toggle] = useToggle(initialValue);
```

Intial value must be a boolean value, `true` or `false`. Default value is `false`.

## Example

### Default True

<Canvas>
  <Story story={defaultOn} />
</Canvas>

### Default False

<Canvas>
  <Story story={defaultOff} />
</Canvas>